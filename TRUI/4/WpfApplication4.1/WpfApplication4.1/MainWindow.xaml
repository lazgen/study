<Window x:Class="WpfApplication3._1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication3._1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">



    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBox Text="text" Grid.Row="0" x:Name="t1" FontSize="{Binding ElementName=slider1, Path=Value}"/>
        <!--Добавил Mode=TwoWay для привязки в обе стороны-->
        <TextBox Grid.Row="1" x:Name="t2" Text="{Binding ElementName=t1, Path=Text, Mode=TwoWay}" />

        <Slider Grid.Row="3" x:Name="slider1" Value="{Binding ElementName=fontSizeTextBox, Path=Text}" Maximum="100"/>
        <Slider Grid.Row="4" x:Name="slider2" Value="{Binding ElementName=slider1, Path=Value}"  Maximum="100"/>


        <TextBox Grid.Row="2" x:Name="fontSizeTextBox" Text="15" />


    </Grid>

</Window>
